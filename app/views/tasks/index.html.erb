<h1>Tasks:</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %><span> | </span><%= link_to '❌', task_path(task), method: :delete %></li>
  <% end %>
</ul>

<span>: </span>

<%= link_to 'add', new_task_path %>
